# tex2pdf GitHub Action

This action aims to simplify automatic compilation of LaTeX documents via GitHub Actions.

## Inputs

### `document`

**Required** The filename of your main document.

## Example usage

```yaml
uses: mxdececco/pdf2tex-gha@v1
with:
  document: main.tex
```
